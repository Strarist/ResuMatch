# Canonical backend environment template: see backend/.env.example
#
# Copy and configure:
#   cp backend/.env.example backend/.env
#
# Local infrastructure (from repo root):
#   docker compose up -d postgres redis
